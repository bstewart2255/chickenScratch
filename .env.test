# Test environment configuration
# This file is used for running tests locally and in CI/CD
# DO NOT use production database credentials here!

NODE_ENV=test
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/signature_auth_test
DB_HOST=localhost
DB_PORT=5432
DB_NAME=signature_auth_test
DB_USER=postgres
DB_PASSWORD=postgres
PGUSER=postgres

# Test-specific settings
API_SECRET_KEY=test-secret-key
JWT_SECRET=test-jwt-secret
LOG_LEVEL=error